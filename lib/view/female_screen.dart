import 'package:flutter/material.dart';

class Femalescreen extends StatelessWidget {
  const Femalescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('female users'),
      ),
      body: const Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
